# Supabase — populated values enable cloud auth + project sync. When
# blank, the app runs in localStorage-only mode (no login screen).
#
# Get these from your project's API settings in the Supabase dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
